"test_id" so that each test can be unique either created by user or created by us.


"id" inside question set so that each question can be recognized individually (basically implementing the feature that user can select questions from already made questions)

"test_duration" in ms so that test duration can be set an clock can be set


"instruction" for set of instruction in test

"question" can be of two type here one with only one answer and other where many answer can be true so that why I made a array of answer

